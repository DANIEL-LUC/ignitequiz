import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
    container: {
        justifyContent: "center",
        alignItems: 'center'
    },
    canvas: {
        width: 257,
        height: 249,
        position: 'absolute',
        zIndex: 1
    }
})